# PostgreSQL Database Configuration
# Used for storing raw and transformed weather data
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=
POSTGRES_HOST=
POSTGRES_PORT=

# Airflow Configuration
# UID for Airflow user (run `id -u` on Linux/Mac to get your user ID)
AIRFLOW_UID=

# Fernet key for encrypting Airflow connections and variables
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
AIRFLOW__CORE__FERNET_KEY=

# WeatherStack API Configuration
# Get your free API key from: https://weatherstack.com/
WEATHERSTACK_API_KEY=
